<%= form_for(@password) do |f| %>
  <% if @password.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@password.errors.count, "error") %> prohibited this password from being saved:</h2>

      <ul>
      <% @password.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :server %><br />
    <%= f.text_field :server %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="field">
    <%= f.label 'Password' %><br />
    <%= text_field_tag 'raw_password', @raw_password %>
  </div>
  <div class="field">
    <%= f.label :groups %><br />
    <%= f.collection_select :group_ids, current_user.groups.sort_by {|group| group.name}, :id, :name,{}, {:multiple=>true, :size=>10} %>
  </div>
  <div class="actions">
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
